node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.DS_Store
.vercel

# The REAL pipeline artefact. Generated by the pipeline as
# `solution/output/dashboard_bundle.json` and copied here by hand (see README §
# "Wiring in the real bundle"). Deliberately not committed so the repo never
# carries a stale copy of run output.
# `public/data/bundle.mock.json` IS committed — it is the fallback the build
# uses when this file is absent.
public/data/bundle.json
